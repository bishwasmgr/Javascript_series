// let
const a = true
if (a== true ){
    let b = 30
    var c = 40
    const d = 50 
}
// const 
// console.log(d) //it doesn't run as it is defined as const inside if statement and it is outside the scope 
console.log(c) // it gets run coz 
// console.log(b) //it doesn't run as it is defined as const inside if statement and it is outside the scope 


// var